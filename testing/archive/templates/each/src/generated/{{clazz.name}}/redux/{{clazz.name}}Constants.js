
module.exports = {
    // CUSTOM_HEADER_START
    // CUSTOM_HEADER_END
    {{upperCase clazz.name}}_CREATE: '{{upperCase clazz.name}}_CREATE',
    {{upperCase clazz.name}}_FIND_ALL: '{{upperCase clazz.name}}_FIND_ALL',
    {{upperCase clazz.name}}_UPDATE: '{{upperCase clazz.name}}_UPDATE',
    {{upperCase clazz.name}}_DELETE: '{{upperCase clazz.name}}_DELETE',
    {{upperCase clazz.name}}_SELECT: '{{upperCase clazz.name}}_SELECT'
};
